import {BigNumber} from "@ethersproject/bignumber";

export const ETH_PRICE = BigNumber.from("1738000000000000000000");
export const FAUCET_MINT_AMOUNT = BigNumber.from(10000);

export const Mantissa = BigNumber.from(10).pow(18);
